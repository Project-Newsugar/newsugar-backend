apiVersion: v1
kind: Service
metadata:
  name: newsugar-backend-service-dev
  namespace: default
  labels:
    app: newsugar-backend
    env: dev
spec:
  type: LoadBalancer
  selector:
    app: newsugar-backend
    env: dev
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
