apiVersion: v1
kind: Service
metadata:
  name: newsugar-backend-service-prod
  namespace: default
  labels:
    app: newsugar-backend
    env: prod
spec:
  type: LoadBalancer
  selector:
    app: newsugar-backend
    env: prod
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
